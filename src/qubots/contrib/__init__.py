"""Optional adapters and helpers that bring third-party data formats and
benchmark suites into the qubots framework.

Each submodule has its own optional dependency. Importing the submodule
without the dependency raises ``ImportError`` with an install hint.
"""
