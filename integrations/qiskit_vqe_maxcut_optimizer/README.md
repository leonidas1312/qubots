# Qiskit VQE MaxCut Optimizer

Small local VQE-style MaxCut optimizer using a hardware-efficient ansatz,
random parameter search, and Qiskit `Statevector` expectation evaluation.

This is intentionally a small-graph integration demo, not a production quantum
workflow. It does not require Aer or cloud hardware.
