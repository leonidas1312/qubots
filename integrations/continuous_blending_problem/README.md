# Continuous Blending Problem

A tiny continuous LP for solver integration tests:

- minimize `2x + 3y`
- require `x + 2y >= 8`
- require `3x + y >= 9`
- `x, y >= 0`

The optimum is `x=2`, `y=3`, objective `13`.
