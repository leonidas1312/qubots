# SciPy Assignment Optimizer

Uses `scipy.optimize.linear_sum_assignment` to solve cost-matrix assignment
problems imported by the qubots `cost_matrix` detector.

```bash
qubots detect examples/pilots/technician_job_costs.csv --family assignment
```
