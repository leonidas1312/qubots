# D-Wave Neal MaxCut Optimizer

Builds a `dimod.BinaryQuadraticModel` for MaxCut and samples it with
`neal.SimulatedAnnealingSampler`. This is a local classical sampler from the
D-Wave Ocean ecosystem, so it runs without quantum hardware access.
